/** Learning Architect overlay — full chat UI in US3 (T024, T025). */
export default function LearningArchitectPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-[var(--text-primary)]">
        Learning Architect
      </h1>
      <p className="mt-2 text-[var(--text-muted)]">
        Socratic chat (quiz vs case study) — full overlay in User Story 3.
      </p>
    </div>
  );
}
